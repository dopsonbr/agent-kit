{
  "name": "agent-kit",
  "version": "0.1.0",
  "description": "CLI for managing AI coding agent configurations",
  "type": "module",
  "bin": {
    "ak": "./dist/cli/index.js"
  },
  "main": "./dist/lib/index.js",
  "types": "./dist/types/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/types/index.d.ts",
      "import": "./dist/lib/index.js"
    },
    "./cli": {
      "types": "./dist/cli/index.d.ts",
      "import": "./dist/cli/index.js"
    }
  },
  "scripts": {
    "dev": "bun run --watch src/cli/index.ts",
    "build": "bun build src/cli/index.ts --outdir dist/cli --target node && bun build src/lib/index.ts --outdir dist/lib --target node",
    "build:standalone": "bun build src/cli/index.ts --compile --outfile dist/ak-darwin-arm64 --target bun-darwin-arm64",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "test:coverage": "bun test --coverage",
    "lint": "oxlint src tests",
    "lint:fix": "oxlint src tests --fix",
    "format": "oxfmt --write .",
    "format:check": "oxfmt --check .",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "bun run build",
    "ak": "bun run src/cli/index.ts"
  },
  "keywords": [
    "cli",
    "ai",
    "agents",
    "claude",
    "copilot",
    "codex",
    "skills",
    "agent-skills",
    "agents-md"
  ],
  "author": "",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/YOUR_ORG/agent-kit.git"
  },
  "homepage": "https://github.com/YOUR_ORG/agent-kit#readme",
  "bugs": {
    "url": "https://github.com/YOUR_ORG/agent-kit/issues"
  },
  "files": [
    "dist",
    "content",
    "README.md",
    "LICENSE"
  ],
  "devDependencies": {
    "@types/bun": "latest",
    "oxfmt": "^0.19.0",
    "oxlint": "^0.16.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "bun@1.0.0"
}
